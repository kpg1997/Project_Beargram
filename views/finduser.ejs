<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=searchMid%> : <%=search%> 검색 결과
    </title>
    <link rel='stylesheet' href='/stylesheets/finduser.css' />
    <link rel="shortcut icon" href="/images/favicon.png" type="image/x-icon">
    <link rel="icon" href="/images/favicon.png" type="image/x-icon">
    <script src="/javascripts/jquery-3.5.1.min.js"></script>
</head>

<body>
    <div id="header">
        <header id="include-header"></header>
    </div>
    <!-- 밥 먹고 유저 상세보기 만들기 -->
    <!-- 유저 상세보기, 유저가 작성한 글 보기 -->
    <div class="panel">
        <div class="empty"></div>
        <div class="searchUser-panel">
            <div class="search-result"><%=searchMid%> : <%=search%> 검색 결과</div>
            <% data.forEach((item,index)=>{%>
                <div class="user-list">
                    <a href="/users/findUser/<%=item.userId%>">
                        <%if(item.userImageUrl==null){%>
                            <%if(item.gender==1){%>
                                <div class="userImage"><img src="/images/men.png" alt=""></div>
                            <%}else{%>
                                <div class="userImage"><img src="/images/girl.png" alt=""></div>
                            <%}%>
                        <%}else{%>
                            <div class="userImage"><img src="/images/<%=item.userId%>/<%=item.userImageUrl%>" alt=""></div>
                        <%}%>
                        <div class="findUser-information">
                            <div class="userId"><%=item.userId%></div>
                            <div class="userName">(<%=item.userName%>)</div>
                        </div>
                    </a>
                </div>

            <%})%>
        </div>
        <div class="empty"></div>
    </div>
    <div class="GoTop">
        <a href="#header">
            <img src="/images/upArrow.png" alt="">
        </a>
    </div>
    <script src="/javascripts/finduser.js"></script>
</body>

</html>