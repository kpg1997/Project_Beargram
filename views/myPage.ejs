<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>마이페이지</title>
    <link rel='stylesheet' href='/stylesheets/myPage.css' />
    <link rel="shortcut icon" href="/images/favicon.png" type="image/x-icon">
    <link rel="icon" href="/images/favicon.png" type="image/x-icon">
    <script src="/javascripts/jquery-3.5.1.min.js"></script>
</head>

<body>
    <div>
        <header id="include-header"></header>
    </div>

    <div class="board-panel">
        <div class="empty"></div>
        <div class="myPrivacy">
            <div class="myPrivacy-form">
                <form enctype="multipart/form-data" action="/users/<%=data.userId%>/<%=data.userImageUrl%>"
                    name="updateForm" method="POST">
                    <div class="imgUrl-panel">
                        <%if(data.userImageUrl==null){%>
                            <%if(data.gender==1 ){%>
                                <img src="/images/men.png" alt="">
                                <%}else{%>
                                    <img src="/images/girl.png" alt="">
                                    <%}%>
                                        <%}else{%>
                                            <img src="/images/<%=data.userId%>/<%=data.userImageUrl%>" alt="">
                                            <%}%>

                    </div>
                    <div class="input-label">
                        <label for="userImageUrl" id="imagess">프로필 변경</label><input type="file" name="userImageUrl" id="userImageUrl" onchange="readImg(this)">
                        <div class="preview-img">
                            <img id="previewProfile">
                        </div>
                    </div>

                    <div class="updateInput-panel">
                        <div>이름<br><input type="text" value="<%=data.userName%>" name="userName" id="userName"></div>
                        <div>비밀번호<br><input type="password" name="userPwd" id="userPwd" value="<%=pwd%>"></div>
                        <div>비밀번호 확인<br><input type="password" id="reuserPwd" value="<%=pwd%>"></div>
                        <div>전화번호<br><input type="text" id="userPhoneNumber" maxlength="11" value="<%=data.userPhoneNumber%>"
                                name="userPhoneNumber"></div>
                        <div>이메일<br><input type="text" id="userEmail" value="<%=data.userEmail%>" name="userEmail"></div>
                    </div>
                    <button id="updateBtn">변경하기</button>
                </form>
            </div>
        </div>
        <div class="empty"></div>
    </div>
    <script src="/javascripts/myPage.js"></script>
</body>

</html>